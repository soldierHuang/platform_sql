# dockerfile.airflow

# 使用一個穩定的 Airflow 官方映像
FROM apache/airflow:2.8.4

# Airflow 官方映像的默認用戶是 airflow
USER airflow

# 複製純淨版的 Airflow 依賴文件
COPY . /opt/airflow/

# 只安裝 Airflow 自身需要的、明確聲明的依賴
RUN pip install --no-cache-dir -r /opt/airflow/requirements-airflow.txt

